[tool.uv.sources]
ommx = { workspace = true }

# JijModeling 2.4.1 is used in the Experiment tutorial only for its
# Problem protobuf codec. Its published metadata still caps OMMX at <3,
# so relax that transitive bound for this workspace's dev lock.
[[tool.uv.dependency-metadata]]
name = "jijmodeling"
version = "2.4.1"
requires-python = ">=3.11,<4"
requires-dist = [
  "numpy",
  "ommx>=2.0.0,<4.0.0",
  "orjson>=3.8.0,<4.0.0",
  "pandas",
  "typing-extensions",
]

[tool.uv.workspace]
members = ["python/ommx*"]

# Keep development-only dependencies at the workspace root. Workspace members
# declare only their runtime and build dependencies.
[dependency-groups]
lint = [
  "ruff>=0.16.0",
]
typecheck = [
  "pyright>=1.1.394",
]
test = [
  "markdown-code-runner>=2.1.0",
  "pytest>=8.3.4",
  "pytest-github-actions-annotate-failures",
  "syrupy>=4.7.2",
]
benchmark = [
  { include-group = "test" },
  "pytest-codspeed>=4.5.0",
]
build = [
  "cibuildwheel",
  "maturin>=1.12.2,<2.0.0",
]
maintenance = [
  "packaging",
  "ruamel-yaml>=0.18.10",
  "tomlkit>=0.13.2",
]
docs = [
  "jijmodeling>=2.4.1,<3.0.0; python_version >= '3.11'",
  "matplotlib>=3.10.7",
  "myst-nb>=1.0.0",
  "sphinx>=7.4.7",
  "sphinx-autoapi>=3.5.0",
  "sphinx-book-theme>=1.1.4",
  "sphinx-fontawesome>=0.0.6",
  "sphinxcontrib-katex>=0.9.11",
  "tomlkit>=0.13.2",
]
release = [
  "twine>=6.0.0,<7.0.0",
]
dev = [
  { include-group = "lint" },
  { include-group = "typecheck" },
  { include-group = "test" },
  { include-group = "benchmark" },
  { include-group = "build" },
  { include-group = "maintenance" },
  { include-group = "docs" },
  { include-group = "release" },
]

[tool.pytest.ini_options]
markers = [
  "benchmark: performance benchmark collected by pytest-codspeed",
  "benchmark_guardrail: persistent regression guardrail run on main and release tags",
  "benchmark_diagnostic: profiling workload run only by the full manual benchmark suite",
]
