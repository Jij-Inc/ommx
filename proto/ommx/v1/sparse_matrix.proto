syntax = "proto3";

package ommx.v1;

// COO format of sparse matrix. The components must have same size.
message SparseMatrix {
  uint64 num_rows = 1;
  uint64 num_columns = 2;
  repeated uint64 rows = 3;
  repeated uint64 columns = 4;
  repeated double values = 5;
}
