syntax = "proto3";

package ommx;

// A monomial in the polynomial.
message Monomial {
    repeated uint64 indices = 1;
    fixed64 coefficient = 2;
}

message Polynomial {
    repeated Monomial terms = 1;
}
