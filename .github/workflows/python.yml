name: Python

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  protogen:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Environment
        uses: ./.github/actions/setup-test-environment

      - name: Regenerate python bindings
        run: task proto:python

      - name: Check updated
        run: |
          git add -N python/ommx/ommx/  # To check newly generated files
          git diff --exit-code

  stub-gen:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Environment
        uses: ./.github/actions/setup-test-environment

      - name: Regenerate stub file
        run: task python:stubgen

      - name: Check updated
        run: |
          git diff --exit-code python/ommx/ommx/_ommx_rust.pyi

  # uv run and maturin develop/build doesn't work well together-
  # Even if one runs maturin develop once, uv run pyright
  # will rebuild the extension, and if we run uv run --no-build pyright,
  # it will fail to find the extension.
  # So, it is better to run pyright separately from the wheel building job.
  pyright:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Setup Environment
        uses: ./.github/actions/setup-test-environment
      - name: Run Pyright
        run: task python:ommx:pyright

  build-wheel:
    name: Build Wheel
    runs-on: ubuntu-latest
    env:
      python-version: 3.12
    steps:
      - uses: actions/checkout@v6

      - name: Setup Environment
        uses: ./.github/actions/setup-test-environment
        with:
          python-version: ${{ env.python-version }}
      - name: Initialise uv
        run: uv venv -p ${{env.python-version}}
      - name: Build ommx wheel
        uses: PyO3/maturin-action@v1
        with:
          command: build
          args: >
            --out wheels
            -m python/ommx/Cargo.toml
      - name: Upload wheel
        uses: actions/upload-artifact@v7
        with:
          name: wheels
          path: wheels

  bridge-v1:
    name: Rust v3 sender to Python 2.9
    runs-on: ubuntu-latest
    needs: build-wheel
    steps:
      - uses: actions/checkout@v6
      - name: Setup Environment
        uses: ./.github/actions/setup-test-environment
        with:
          python-version: "3.12"
      - name: Create isolated receiver environment
        run: uv venv --python 3.12 .venv-bridge
      - name: Check generated sender stub
        run: |
          PYO3_PYTHON="$PWD/.venv-bridge/bin/python" cargo run --locked --manifest-path python/bridge-tests/modeling/Cargo.toml --bin stub_gen
          uvx --from 'ruff==0.9.10' ruff format python/bridge-tests/modeling/bridge_test_modeling.pyi
          git diff --exit-code -- python/bridge-tests/modeling/bridge_test_modeling.pyi
      - name: Build sender wheel
        run: >
            uvx --from 'maturin>=1.12.2,<2' maturin build
            --locked --out bridge-wheels
            --interpreter .venv-bridge/bin/python
            -m python/bridge-tests/modeling/Cargo.toml
      - name: Download receiver wheel
        uses: actions/download-artifact@v8
        with:
          name: wheels
          path: wheels
      - name: Install independent sender and receiver wheels
        run: uv pip install --python .venv-bridge/bin/python wheels/*.whl bridge-wheels/*.whl pytest pyright
      - name: Type check bridge integration
        run: uv run --no-project --python .venv-bridge/bin/python pyright --pythonpath .venv-bridge/bin/python python/bridge-tests
      - name: Test bridge integration
        run: uv run --no-project --python .venv-bridge/bin/python pytest -v python/bridge-tests

  # Test a single wheel with multiple python versions
  test:
    runs-on: ubuntu-latest
    needs: build-wheel
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12", "3.13", "3.14"]
    steps:
      - uses: actions/checkout@v6

      - name: Setup Environment
        uses: ./.github/actions/setup-test-environment
        with:
          python-version: ${{ matrix.python-version }}
      - name: Download wheel
        uses: actions/download-artifact@v8
        with:
          name: wheels
          path: wheels
      - name: Transform Python Project to run tests
        run: |
          uv run ci-scripts/setup-python-test-project.py ${{ matrix.python-version }}
          cat pyproject.toml
      - name: Install deps
        run: task python:sync
      - name: Test
        run: task python:test-ci

  ruff:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Setup Environment
        uses: ./.github/actions/setup-test-environment

      - name: Format
        run: uvx ruff format --check python

      - name: Lint
        run: uvx ruff check python
