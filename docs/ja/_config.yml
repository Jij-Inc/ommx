# Book settings
# Learn more at https://jupyterbook.org/customize/config.html

title: OMMX
author: Jij Inc.
logo: ../logo.png

# Notebook execution settings
# See https://jupyterbook.org/content/execute.html
# Default is 'off' for fast builds; CI tests use 'force' via --config option
execute:
  execute_notebooks: off

# Define the name of the latex output file for PDF builds
latex:
  latex_documents:
    targetname: book.tex

# Information about where the book exists on the web
repository:
  url: https://github.com/Jij-Inc/ommx # Online location of your book
  path_to_book: docs/ja # Optional path to your book, relative to the repository root
  branch: main # Which branch of the repository should be used when creating links (optional)

# Add GitHub buttons to your book
# See https://jupyterbook.org/customize/config.html#add-a-link-to-your-repository
html:
  favicon: "../logo.png"
  use_issues_button: true
  use_repository_button: true
  analytics:
    google_analytics_id: GA4_TRACKING_ID

# launch_buttons disabled since notebooks are now MyST Markdown
# launch_buttons:
#   notebook_interface: "classic"
#   binderhub_url: "https://mybinder.org"
#   colab_url: "https://colab.research.google.com"
