# .readthedocs.yaml
# Read the Docs configuration file for Jupyter Book 2 (MyST)
# See https://mystmd.org/guide/deployment-readthedocs for details

version: 2
build:
  os: ubuntu-24.04
  tools:
    python: "3.11"
    nodejs: "22"
  commands:
    # Install uv
    - pip install uv
    # Install dependencies
    - cd docs && uv sync
    # Build the site
    - cd docs/ja && uv run jupyter book build --html
    # Copy the output to Read the Docs expected location
    - mkdir -p $READTHEDOCS_OUTPUT/html/
    - cp -r docs/ja/_build/html/. "$READTHEDOCS_OUTPUT/html"
